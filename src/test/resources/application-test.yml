spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/devmentor
    driver-class-name: org.postgresql.Driver
    username: devmentor
    password: devmentor123

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Disable AI calls in tests
ai:
  openai:
    api-key: test-key

app:
  jwt:
    secret: test-secret-key-for-testing-minimum-256-bits-required
    expiration: 86400000

logging:
  level:
    root: WARN
    com.devmentor: DEBUG
