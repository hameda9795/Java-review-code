version: '3.8'

services:
  # PostgreSQL database with pgvector extension
  postgres:
    image: pgvector/pgvector:pg16
    container_name: devmentor-postgres
    environment:
      POSTGRES_DB: devmentor
      POSTGRES_USER: devmentor
      POSTGRES_PASSWORD: devmentor123
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - devmentor-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U devmentor"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Spring Boot application (uncomment when ready to dockerize)
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: devmentor-app
  #   environment:
  #     SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/devmentor
  #     SPRING_DATASOURCE_USERNAME: devmentor
  #     SPRING_DATASOURCE_PASSWORD: devmentor123
  #     OPENAI_API_KEY: ${OPENAI_API_KEY}
  #     GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
  #     GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
  #     JWT_SECRET: ${JWT_SECRET}
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - devmentor-network

  # Prometheus for metrics (optional)
  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: devmentor-prometheus
  #   volumes:
  #     - ./prometheus.yml:/etc/prometheus/prometheus.yml
  #     - prometheus_data:/prometheus
  #   ports:
  #     - "9090:9090"
  #   networks:
  #     - devmentor-network

  # Grafana for dashboards (optional)
  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: devmentor-grafana
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=admin
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - grafana_data:/var/lib/grafana
  #   networks:
  #     - devmentor-network
  #   depends_on:
  #     - prometheus

volumes:
  postgres_data:
  prometheus_data:
  grafana_data:

networks:
  devmentor-network:
    driver: bridge
